import { View } from 'react-native';

// This is a dummy component that will never be shown
// The logout tab intercepts the press and shows the logout alert
export default function LogoutScreen() {
  return <View />;
}